API docs generation
===================

Run the following to generate API rst files and build the docs:

```bash
./docs/generate_apidoc.sh
python -m pip install -r docs/requirements.txt
python -m sphinx -b html docs docs/_build/html
```

The script will create `docs/api/` with `modules.rst` and module pages generated by `sphinx-apidoc`.
